# Deferred items for Revision 7 --- STATUS: ADDRESSED

**Revision 7 is drafted at `paper/eoc_rev7.tex` (26 pp).** Every item below has been carried into it
except where marked otherwise. Revision 6 remains frozen at `paper/eoc_rev6.tex`
([10.5281/zenodo.22903860](https://doi.org/10.5281/zenodo.22903860)); Revision 7 has **no DOI yet**
--- depositing it is a manual step, and no identifier is recorded here until one exists.

| # | Item | Where it landed in Rev 7 |
|---|---|---|
| 1 | ZCRE counterpoint to the cardinality barrier | `rem:zcrecounter`, plus narrowed section title, closed-route row 11, map table and conclusion |
| 2 | Two closed-route labels stronger than their support | row 1 relabelled `T/C` with terminality attributed to argument + scan; row 10 reworded |
| 3 | Transport row understates a positive result | left as is --- the row already records the `O(log n_0)` bound |
| 4 | Open Problem F stateable quantitatively | `prop:entry`, `prop:episodes`, `rem:episodescope` |
| 5 | (U) implies EOC outright | `prop:occstop`, `rem:Uallscope` |
| 6 | `prop:lastmax` status label over-claimed | `sec:lean` now lists `ZeroConfinedSeed` |
| 7 | Two repository defects | fixed on their branches; not manuscript matters |
| 8 | Curry: cite the audited version, separate the two roles | bibliography entry dated with DOI; `rem:divledger` |
| 9 | Do not assert a drift rate for divergent orbits | no rate is asserted anywhere in Rev 7 |
| 10 | The Curry dependency is one theorem, and it is named | `rem:divledger` |

## Still open after Revision 7

* **Revision 7 has not been deposited.** No DOI, and none is to be written into the repository
  until the deposit exists.
* Formalizing Theorem `thm:GTC` (Curry's windowed sparsity) --- the remaining external input.
* The analytic absorption inside `cor:Uall` (`rem:Uallscope`(ii)) --- unformalized.
* `(DE)` itself. Unchanged, and the point of `rem:divledger` is that nothing above touches it.
