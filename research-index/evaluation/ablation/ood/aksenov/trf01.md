# Context packet — 'Could a technique from the Riemann hypothesis literature help the EOC arithmetic bottleneck?'

Generated by `tools/research_context` · retrieval mode `aksenov`. **This is an index, not evidence.** Any claim used in a proof or a novelty assessment must be decompressed back to the cited Lean declaration or document and verified there.

## Treated as a transfer request

This question names mathematics outside the repository (Riemann hypothesis) and asks whether it carries over. The repository material below is offered **as the nearest EOC-side context for such a comparison, not as evidence that any connection exists.** No result below mentions Riemann hypothesis; the analogy is yours to justify.

## Most relevant research nodes

### FiniteValuationWord (the central datatype)

The valuation-word abstraction on which most of the library is built; by a wide margin the most depended-upon EOC declaration (425 reverse dependencies inside EOC).

- **source:** EOC/FiniteValuationWord.lean · `EOC.FiniteValuationWord`
- **score 1.2545** = relevance 0.25 + keyword 0.0 + centrality 1.0 + compression 0.009 · matched eoc

### Chain A arithmetic bottleneck: PowerOfTwoDangerousWindowSparsity

The documented open target: prove PowerOfTwoDangerousWindowSparsity for all sufficiently large even j, every shift t in range, and some logarithmic K <= A*log2(j)+1; the lambda = 1 case is the essential core. Separately open and needed by the exceptional-set chain: lower shells sigma < floor(j*alpha) and barrier offsets U > 0. The document states explicitly that it is NOT proved, NOT known to follow from any equidistribution theorem, and NOT claimed equivalent to any named conjecture.

- **source:** docs/ARITHMETIC_FRONTIER.md · Sec. 10 Open mathematical target (OPEN)
- **score 0.75** = relevance 0.5 + keyword 0.25 + centrality 0.0 + compression 0.0 · matched arithmetic, bottleneck

### Curry hypothesis (ReciprocalSummable)

Summability of the reciprocals along the orbit. Everything in the Curry sector is conditional on this and must be labelled as such -- it is an assumed input, not a theorem of the repository.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.ReciprocalSummable`
- **score 0.266** = relevance 0.25 + keyword 0.0 + centrality 0.0099 + compression 0.0122 · matched eoc

### Deficit recursion (pure algebra)

Delta_{k+1} = Delta_k + b_{k+1} - d_k, where b is the Beatty gap and d the valuation digit. Its Lean proof is `unfold deficit; push_cast; ring`. This is the canonical example of a recurrence that looks dynamical and carries no information -- see the coordinate-change gate.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.deficit_succ`
- **score 0.2529** = relevance 0.25 + keyword 0.0 + centrality 0.0001 + compression 0.0057 · matched eoc

### Bounded prefix realizers = positive-integer realizability

sup_N r_N < infinity  <=>  the infinite valuation word has a positive-integer realizer  <=>  r_N is eventually constant. Unconditional, and independent of the Curry sector. Consequence: qualitative unbounded-realizer growth is NOT an easier target than positive-integer realizability, and must not be pursued as if it were.

- **source:** EOC/ZCRERealizerGrowth.lean · `EOC.ZCRERealizerGrowth.boundedPrefixRealizers_iff_positiveRealizer`
- **score 0.2527** = relevance 0.25 + keyword 0.0 + centrality 0.0001 + compression 0.0052 · matched eoc

### Least prefix realizer stabilizes

Bounded least-prefix-realizers are eventually constant. For an actual positive seed m_0 this means r_N = m_0 once 2^{S_N+1} > m_0 -- an extremely rigid signature, but not a mechanism for forbidding stabilization.

- **source:** EOC/ZCRERealizerGrowth.lean · `EOC.ZCRERealizerGrowth.leastRealizer_eventually_constant_of_bounded`
- **score 0.2522** = relevance 0.25 + keyword 0.0 + centrality 0.0002 + compression 0.0038 · matched eoc

## Formal declarations worth opening

| declaration | module | depth | log2 unwrapped | rev-deps | why |
|---|---|---|---|---|---|
| `FiniteValuationWord` | EOC.FiniteValuationWord | 1 | 2 | 425 | matched eoc |
| `EventProb` | EOC.TaoLike.TaoInterface | 1 | 2 | 28 | matched eoc |
| `G` | EOC.SwapBound | 5 | 9 | 11 | matched eoc |
| `genEventProb` | EOC.TaoLike.TaoInterface | 2 | 4 | 58 | matched eoc |
| `IsProbabilityLaw` | EOC.TaoLike.TaoInterface | 3 | 6 | 17 | matched eoc |
| `congr_simp` | EOC.CapacityBounds | 6 | 9 | 0 | matched eoc |
| `congr_simp` | EOC.LocalWindow | 5 | 8 | 0 | matched eoc |
| `congr_simp` | EOC.LocalWindow | 4 | 8 | 0 | matched eoc |

Open with the file and line from `research-index/formal_graph.json`.

## Before you proceed

Apply the coordinate-change gate in `research-index/research_protocol.md`: if the quantity you are about to introduce is a deterministic function of the digit word or the orbit state, the recurrence you derive for it will be provable by `ring`, and it carries no new restriction.