# Context packet — 'Does the Riemann hypothesis imply anything here?'

Generated by `tools/research_context` · retrieval mode `baseline_mvp`. **This is an index, not evidence.** Any claim used in a proof or a novelty assessment must be decompressed back to the cited Lean declaration or document and verified there.

## Most relevant research nodes

### Tao-style machinery is gated on an unproved hypothesis  ·  `BOTTLENECK`

The repository's Chain A Fourier / dangerous-window machinery is a CONDITIONAL result gated on TaoMixingHypothesis, an unproved external interface. It does not currently reach the realizability frontier, and re-running it there will not change that without progress on the hypothesis itself.

- **source:** docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md · Sec. 27 Routes already known not to work, or not new
- **score 0.55** = relevance 0.2 + keyword 0.1 + status prior 0.25 · matched hypothesis

### Curry hypothesis (ReciprocalSummable)  ·  `CONDITIONAL_FOUNDATION`

Summability of the reciprocals along the orbit. Everything in the Curry sector is conditional on this and must be labelled as such -- it is an assumed input, not a theorem of the repository.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.ReciprocalSummable`
- **score 0.5** = relevance 0.2 + keyword 0.1 + status prior 0.2 · matched hypothesis

### The surviving sector: zero-corridor, unboundedly many defects, diverging deficit  ·  `CURRENT_FRONTIER`

Population C in the frontier document: the sector that remains open after the controlled sectors are removed. The moving-anchor obstruction is itself the open problem here, not a tool for attacking it.

- **source:** docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md · Sec. 23 Sectors already controlled
- **score 0.45** = relevance 0.2 + keyword 0.0 + status prior 0.25 · matched here

## Relations touching these nodes

- `curry-zero-corridor` --**CONDITIONAL_ON**--> `curry-hypothesis` — the normalization holds only under reciprocal summability [EOC/CurryFoundation.lean]
- `drift-to-minus-infinity` --**CONDITIONAL_ON**--> `curry-hypothesis` — CURRY + ELEMENTARY CONSEQUENCE, per the foundation document [EOC/CurryFoundation.lean]
- `zcre-realizer-equivalence` --**BLOCKED_BY**--> `zcre-frontier` — the equivalence removes a proposed easier route; the frontier is unchanged by it [docs/ZCRE_REALIZER_GROWTH_AUDIT.md]
- `tao-mixing-conditional` --**BLOCKED_BY**--> `chain-a-bottleneck` — Chain A machinery is gated on an unproved external hypothesis [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-pell-continued-fraction` --**CLOSES_ROUTE**--> `zcre-frontier` — listed as not to be repeated without a stated realizability connection [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-fixed-modulus-packing` --**CLOSES_ROUTE**--> `zcre-frontier` — terminal at B < 8/9 independently of Curry [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-entropy-counting` --**CLOSES_ROUTE**--> `zcre-frontier` — fully spent on the window-sparsity axis [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-periodic-only` --**CLOSES_ROUTE**--> `zcre-frontier` — the surviving sector is defined by excluding periodicity [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-bounded-defect-only` --**REFUTES**--> `zcre-frontier` — naive recovery shown not to extend the bounded-defect mechanism [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `discriminator-criterion` --**BLOCKED_BY**--> `zcre-frontier` — any candidate invariant must pass this bar before effort is spent [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]

## Before you proceed

Apply the coordinate-change gate in `research-index/research_protocol.md`: if the quantity you are about to introduce is a deterministic function of the digit word or the orbit state, the recurrence you derive for it will be provable by `ring`, and it carries no new restriction.