# Context packet — 'What lessons transfer from Navier-Stokes blowup analysis to Type-II divergence?'

Generated by `tools/research_context` · retrieval mode `semantic_only`. **This is an index, not evidence.** Any claim used in a proof or a novelty assessment must be decompressed back to the cited Lean declaration or document and verified there.

## Treated as a transfer request

This question names mathematics outside the repository (Navier-Stokes) and asks whether it carries over. The repository material below is offered **as the nearest EOC-side context for such a comparison, not as evidence that any connection exists.** No result below mentions Navier-Stokes; the analogy is yours to justify.

## Most relevant research nodes

### Type-II divergent orbit (the target)  ·  `CURRENT_FRONTIER`

The object the repository exists to exclude: an accelerated Collatz orbit that diverges. Everything below is either a reduction of this, a conditional handle on it, or a route to it that has been closed.

- **source:** docs/RESEARCH_STATUS.md
- **score 0.25** = relevance 0.3333 + keyword 0.0 + status prior 0.25 · matched divergence, ii, type

### Curry normalization to the zero corridor  ·  `CONDITIONAL_FOUNDATION`

Conditionally on Curry, a hypothetical Type-II orbit can be restarted at the last global drift maximum so that R_0 = 0 and R_N < 0 for N >= 1, hence S_N <= floor(N*alpha) and the deficit is non-negative. This is the normalization every later Curry-sector argument assumes.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.zero_corridor_tail`
- **score 0.2** = relevance 0.2222 + keyword 0.0 + status prior 0.2 · matched ii, type

### Deficit divergence is equivalent to drift divergence  ·  `MAJOR_REDUCTION`

R'_k = -Delta_k - {k*alpha} with the fractional correction bounded in (-1,0], so R'_k -> -infinity is exactly Delta_k -> infinity. This is the coordinate identity that lets the Curry sector be studied in deficit coordinates at all.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.deficit_tendsto_atTop_iff`
- **score 0.15** = relevance 0.1111 + keyword 0.0 + status prior 0.15 · matched divergence

### Drift tends to minus infinity under Curry  ·  `MAJOR_REDUCTION`

R_N -> -infinity for every reciprocally-summable accelerated orbit. Labelled CURRY + ELEMENTARY CONSEQUENCE in the foundation document: not stated by Curry, but a short rigorous consequence via the Eliahou-Rozier identity.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.R_tendsto_atBot`
- **score 0.15** = relevance 0.1111 + keyword 0.0 + status prior 0.15 · matched divergence

### Chain A arithmetic bottleneck: PowerOfTwoDangerousWindowSparsity  ·  `BOTTLENECK`

The documented open target: prove PowerOfTwoDangerousWindowSparsity for all sufficiently large even j, every shift t in range, and some logarithmic K <= A*log2(j)+1; the lambda = 1 case is the essential core. Separately open and needed by the exceptional-set chain: lower shells sigma < floor(j*alpha) and barrier offsets U > 0. The document states explicitly that it is NOT proved, NOT known to follow from any equidistribution theorem, and NOT claimed equivalent to any named conjecture.

- **source:** docs/ARITHMETIC_FRONTIER.md · Sec. 10 Open mathematical target (OPEN)
- **score 0.0** = relevance 0.0 + keyword 0.0 + status prior 0.25

### Bounded-defect-only arguments and naive recovery  ·  `REFUTED_ROUTE`

Proposition 5.10's mechanism is fully used and shown NOT to extend via naive recovery (Remark 5.11). Recovery-efficiency alone is the specific mechanism Remark 5.11 refuted.

- **source:** docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md · Sec. 27 Routes already known not to work, or not new
- **score 0.0** = relevance 0.0 + keyword 0.0 + status prior 0.3

## Relations touching these nodes

- `curry-zero-corridor` --**CONDITIONAL_ON**--> `curry-hypothesis` — the normalization holds only under reciprocal summability [EOC/CurryFoundation.lean]
- `drift-to-minus-infinity` --**CONDITIONAL_ON**--> `curry-hypothesis` — CURRY + ELEMENTARY CONSEQUENCE, per the foundation document [EOC/CurryFoundation.lean]
- `curry-zero-corridor` --**REDUCES_TO**--> `type-ii-divergence` — normalizes a hypothetical Type-II orbit into the zero corridor [docs/CURRY_FOUNDATION.md]
- `deficit-diverges` --**EQUIVALENT_TO**--> `drift-to-minus-infinity` — Delta_k -> infinity iff R'_k -> -infinity; bounded fractional correction [EOC/CurryFoundation.lean]
- `dynamic-deficit-feedback-closed` --**EQUIVALENT_TO**--> `type-ii-divergence` — the feedback system is Type-II divergence in (m_N, Delta_N) coordinates [docs/DYNAMIC_DEFICIT_FEEDBACK_AUDIT.md]
- `tao-mixing-conditional` --**BLOCKED_BY**--> `chain-a-bottleneck` — Chain A machinery is gated on an unproved external hypothesis [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `closed-bounded-defect-only` --**REFUTES**--> `zcre-frontier` — naive recovery shown not to extend the bounded-defect mechanism [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `finite-valuation-word` --**FORMALIZES**--> `type-ii-divergence` — the datatype in which symbolic statements about orbits are expressed [EOC/FiniteValuationWord.lean]

## Before you proceed

Apply the coordinate-change gate in `research-index/research_protocol.md`: if the quantity you are about to introduce is a deterministic function of the digit word or the orbit state, the recurrence you derive for it will be provable by `ring`, and it carries no new restriction.