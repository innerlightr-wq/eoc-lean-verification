# Context packet — 'What does Curry contribute to excluding Type-II divergence?'

Generated by `tools/research_context` · retrieval mode `lexical`. **This is an index, not evidence.** Any claim used in a proof or a novelty assessment must be decompressed back to the cited Lean declaration or document and verified there.

## Most relevant research nodes

### Curry normalization to the zero corridor

Conditionally on Curry, a hypothetical Type-II orbit can be restarted at the last global drift maximum so that R_0 = 0 and R_N < 0 for N >= 1, hence S_N <= floor(N*alpha) and the deficit is non-negative. This is the normalization every later Curry-sector argument assumes.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.zero_corridor_tail`
- **score 0.5833** = relevance 0.5 + keyword 0.0833 · matched curry, ii, type

### Type-II divergent orbit (the target)

The object the repository exists to exclude: an accelerated Collatz orbit that diverges. Everything below is either a reduction of this, a conditional handle on it, or a route to it that has been closed.

- **source:** docs/RESEARCH_STATUS.md
- **score 0.5833** = relevance 0.5 + keyword 0.0833 · matched divergence, ii, type

### Drift tends to minus infinity under Curry

R_N -> -infinity for every reciprocally-summable accelerated orbit. Labelled CURRY + ELEMENTARY CONSEQUENCE in the foundation document: not stated by Curry, but a short rigorous consequence via the Eliahou-Rozier identity.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.R_tendsto_atBot`
- **score 0.5** = relevance 0.3333 + keyword 0.1667 · matched curry, divergence

### Deficit divergence is equivalent to drift divergence

R'_k = -Delta_k - {k*alpha} with the fractional correction bounded in (-1,0], so R'_k -> -infinity is exactly Delta_k -> infinity. This is the coordinate identity that lets the Curry sector be studied in deficit coordinates at all.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.deficit_tendsto_atTop_iff`
- **score 0.3333** = relevance 0.3333 + keyword 0.0 · matched curry, divergence

### Curry hypothesis (ReciprocalSummable)

Summability of the reciprocals along the orbit. Everything in the Curry sector is conditional on this and must be labelled as such -- it is an assumed input, not a theorem of the repository.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.ReciprocalSummable`
- **score 0.25** = relevance 0.1667 + keyword 0.0833 · matched curry

### Generic entropy counting

Already fully spent on the Curry / window-sparsity axis; nothing left to extract from entropy counting alone for the residue axis.

- **source:** docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md · Sec. 27 Routes already known not to work, or not new
- **score 0.1667** = relevance 0.1667 + keyword 0.0 · matched curry

## Before you proceed

Apply the coordinate-change gate in `research-index/research_protocol.md`: if the quantity you are about to introduce is a deterministic function of the digit word or the orbit state, the recurrence you derive for it will be provable by `ring`, and it carries no new restriction.