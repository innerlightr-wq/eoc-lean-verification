# Context packet — 'Can unbounded prefix-realizer growth provide a weaker route than EOC?'

Generated by `tools/research_context` · retrieval mode `semantic_only`. **This is an index, not evidence.** Any claim used in a proof or a novelty assessment must be decompressed back to the cited Lean declaration or document and verified there.

## Most relevant research nodes

### Pell / continued-fraction drift approximants  ·  `CLOSED_ROUTE`

Listed among routes not to repeat. The documented reason is precise and narrow: NO REPOSITORY RESULT CONNECTS THESE TO REALIZABILITY; the route would have to be developed from scratch with a stated realizability connection, not assumed. The repository does NOT document any stronger closure (e.g. a proved decoupling of real approximation from 2-adic behaviour) -- see known_gaps.

- **source:** docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md · Sec. 27 Routes already known not to work, or not new
- **score 0.3** = relevance 0.125 + keyword 0.0 + status prior 0.3 · matched route

### Dynamic deficit feedback is a reparameterization  ·  `CLOSED_ROUTE`

The loop Delta_N -> m_N -> d_N = v_2(3m_N+1) -> Delta_{N+1} is the Bernstein-Lagarias 2-adic conjugacy in deficit coordinates; every arrow is a definition or an already-proved identity. The state-size ceiling d_N <= log2(3m_N+1) is strictly WEAKER than the zero-corridor ceiling d_N <= Delta_N + b for every seed m_0 >= 2, so a large state constrains the next valuation less than the corridor already does.

- **source:** docs/DYNAMIC_DEFICIT_FEEDBACK_AUDIT.md · D. Tautology gate
- **score 0.3** = relevance 0.125 + keyword 0.0 + status prior 0.3 · matched weaker

### Least prefix realizer stabilizes  ·  `EQUIVALENCE`

Bounded least-prefix-realizers are eventually constant. For an actual positive seed m_0 this means r_N = m_0 once 2^{S_N+1} > m_0 -- an extremely rigid signature, but not a mechanism for forbidding stabilization.

- **source:** EOC/ZCRERealizerGrowth.lean · `EOC.ZCRERealizerGrowth.leastRealizer_eventually_constant_of_bounded`
- **score 0.3** = relevance 0.375 + keyword 0.0 + status prior 0.3 · matched eoc, prefix, realizer

### Bounded prefix realizers = positive-integer realizability  ·  `EQUIVALENCE`

sup_N r_N < infinity  <=>  the infinite valuation word has a positive-integer realizer  <=>  r_N is eventually constant. Unconditional, and independent of the Curry sector. Consequence: qualitative unbounded-realizer growth is NOT an easier target than positive-integer realizability, and must not be pursued as if it were.

- **source:** EOC/ZCRERealizerGrowth.lean · `EOC.ZCRERealizerGrowth.boundedPrefixRealizers_iff_positiveRealizer`
- **score 0.3** = relevance 0.625 + keyword 0.0 + status prior 0.3 · matched eoc, growth, prefix, realizer, unbounded

### Type-II divergent orbit (the target)  ·  `CURRENT_FRONTIER`

The object the repository exists to exclude: an accelerated Collatz orbit that diverges. Everything below is either a reduction of this, a conditional handle on it, or a route to it that has been closed.

- **source:** docs/RESEARCH_STATUS.md
- **score 0.25** = relevance 0.125 + keyword 0.0 + status prior 0.25 · matched route

### Curry hypothesis (ReciprocalSummable)  ·  `CONDITIONAL_FOUNDATION`

Summability of the reciprocals along the orbit. Everything in the Curry sector is conditional on this and must be labelled as such -- it is an assumed input, not a theorem of the repository.

- **source:** EOC/CurryFoundation.lean · `EOC.CurryFoundation.ReciprocalSummable`
- **score 0.2** = relevance 0.125 + keyword 0.0 + status prior 0.2 · matched eoc

## Relations touching these nodes

- `curry-zero-corridor` --**CONDITIONAL_ON**--> `curry-hypothesis` — the normalization holds only under reciprocal summability [EOC/CurryFoundation.lean]
- `drift-to-minus-infinity` --**CONDITIONAL_ON**--> `curry-hypothesis` — CURRY + ELEMENTARY CONSEQUENCE, per the foundation document [EOC/CurryFoundation.lean]
- `curry-zero-corridor` --**REDUCES_TO**--> `type-ii-divergence` — normalizes a hypothetical Type-II orbit into the zero corridor [docs/CURRY_FOUNDATION.md]
- `dynamic-deficit-feedback-closed` --**EQUIVALENT_TO**--> `type-ii-divergence` — the feedback system is Type-II divergence in (m_N, Delta_N) coordinates [docs/DYNAMIC_DEFICIT_FEEDBACK_AUDIT.md]
- `dynamic-deficit-feedback-closed` --**DEPENDS_ON**--> `deficit-recursion` — the decisive evidence is that the recursion's Lean proof is `ring` [docs/DYNAMIC_DEFICIT_FEEDBACK_AUDIT.md]
- `coordinate-change-gate` --**CLOSES_ROUTE**--> `dynamic-deficit-feedback-closed` — the gate is distilled from this closure and would have caught it early [research-index/research_protocol.md]
- `coordinate-change-gate` --**CLOSES_ROUTE**--> `zcre-realizer-equivalence` — the gate is distilled from this closure too [research-index/research_protocol.md]
- `realizer-eventually-constant` --**DEPENDS_ON**--> `zcre-realizer-equivalence` — one of the three equivalent conditions [EOC/ZCRERealizerGrowth.lean]
- `zcre-realizer-equivalence` --**BLOCKED_BY**--> `zcre-frontier` — the equivalence removes a proposed easier route; the frontier is unchanged by it [docs/ZCRE_REALIZER_GROWTH_AUDIT.md]
- `closed-pell-continued-fraction` --**CLOSES_ROUTE**--> `zcre-frontier` — listed as not to be repeated without a stated realizability connection [docs/ZERO_CORRIDOR_REALIZABILITY_FRONTIER.md]
- `finite-valuation-word` --**FORMALIZES**--> `type-ii-divergence` — the datatype in which symbolic statements about orbits are expressed [EOC/FiniteValuationWord.lean]

## Before you proceed

Apply the coordinate-change gate in `research-index/research_protocol.md`: if the quantity you are about to introduce is a deterministic function of the digit word or the orbit state, the recurrence you derive for it will be provable by `ring`, and it carries no new restriction.